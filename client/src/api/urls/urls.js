// Base URL for the API
const BASE_API_URL = "http://localhost:3030";
const DATA = "data";

const AUTH_URL = `${BASE_API_URL}/users`;
const POSTS_ENDPOINT = `${BASE_API_URL}/${DATA}/posts`;

export { BASE_API_URL, AUTH_URL, POSTS_ENDPOINT };
